
// did not work same username issue
GIT_AUTHOR_EMAIL="tjvishwa88@email.com" && GIT_AUTHOR_NAME="Tj_" && git commit


//username and email for git status and git init

git config --global user.email "tjvishwa88@gmail.com"

git config --global user.name "Tj_"




// commit to git

git add .
git commit -m "comment"


{/* <h1>User Profile</h1> */}